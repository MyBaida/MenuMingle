# ---- Python / Django ----
__pycache__/
*.py[cod]
*$py.class
venv/
.env
.env.local
.env.*.local
!.env.example
db.sqlite3
db.sqlite3-journal
# collectstatic output (generated on the host during build)
/RestaurantMenu/staticfiles/

# ---- Node / React (Create React App) ----
node_modules/
# CRA production output only. Anchored so it does NOT match the vendored
# frontend/ckeditor5/build/ (see note below).
/frontend/build/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache

# ---- OS / editor ----
.DS_Store
Thumbs.db

# ---------------------------------------------------------------------------
# DO NOT ignore these — they must stay tracked:
#   * frontend/ckeditor5/build/  -> vendored custom build referenced by
#     package.json ("ckeditor5-custom-build": "file:ckeditor5").
#   * RestaurantMenu/static/images/ -> seeded menu photos served in production.
# ---------------------------------------------------------------------------
